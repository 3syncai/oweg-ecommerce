import { defineWidgetConfig } from "@medusajs/admin-sdk"

export const config = defineWidgetConfig({
  zone: "login.before",
})

export default function LoginBrandingWidget() {
  return null
}
